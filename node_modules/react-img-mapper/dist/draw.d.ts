import { CTX } from './types';
declare const callingFn: (shape: string, coords: number[], fillColor: string, lineWidth: number, strokeColor: string, isAreaActive: boolean, ctx: CTX) => void | boolean;
export default callingFn;
